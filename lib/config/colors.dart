import 'package:flutter/material.dart';

class AppColors {
  // Primary accent color for the app
  static const Color accentColor = Color(0xFF1877F2);

  // You can add more colors as needed
  static const Color astrologerCardbg = Color(0xFFF1F4F5);
  static const Color secondaryColor = Color(0xFFF5F5F5);

  // Optional: Dark mode or light mode specific colors
  static const Color darkAccentColor = Color(0xFF7B68EE);
}
